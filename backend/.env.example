# Razorpay Webhook Secret
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret_here

# Firebase Service Account (paste entire JSON as one-line string)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project",...}

# Server Port
PORT=3000

# Instructions:
# 1. Get webhook secret from Razorpay Dashboard → Settings → Webhooks
# 2. Download Firebase service account JSON from Firebase Console
# 3. Copy entire JSON content and paste as single line in FIREBASE_SERVICE_ACCOUNT
# 4. For Railway: Use environment variable editor to paste JSON directly
